package bank.project;
public class test
{
public static void main(String[] args)
{
Read_Online read = new Read_Online();
read.connect("google");
    /*boolean p=true;
    boolean q=true;
    boolean r=false;
    boolean s=true;


        System.out.println((p||q||r)&&(p|| !q || !s) && (q || !r || s) && (!p || r|| s) && (!p || q || !s) && (p || !q || !r) && (!p || !q || s) && (!p || !r || !s));
*/

}
}